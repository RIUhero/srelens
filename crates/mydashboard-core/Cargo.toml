[package]
name = "srelens-mydashboard-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Provider-free read-only MyDashboard Core process contract"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
